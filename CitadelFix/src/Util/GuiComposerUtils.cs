using System;
using System.Collections.Generic;
using System.Linq;
using Vintagestory.API.Client;
using Vintagestory.GameContent;

namespace CitadelFix.Util;

public static class GuiComposerUtils {

    /*
    public static GuiComposer AddFlatList<T>(this GuiComposer composer, ElementBounds bounds, Action<int> onleftClick = null, List<T> stacks = null, string key = null) where T : IFlatListItem
    {
        if (!composer.Composed)
        {
            composer.AddInteractiveElement(new GuiElementFlatList(composer.Api, bounds, onleftClick, stacks as List<IFlatListItem>), key);
        }

        return composer;
    }
    */
}